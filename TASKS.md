# Tasks (Auto-generated)
## 2026-01-30
- [x] 在 `README.md:23` 的“Key options”中补充 `--target-kb` 是上限的说明，并描述当 `compress.py:107` 迭代质量/尺寸调到最小仍大于目标时的提示逻辑，确保文档与实际行为一致（对应 `PLAN.md:26` 的“对外契约”）。
- [x] 参考 `compress.py:97` 的默认格式逻辑和 `--bg-color` 限定用途，在 `README.md:23` 中补充默认输出 JPEG、带透明则改 WebP 以及背景色仅在 JPEG/WebP 有效的说明，保持与 `PLAN.md:26` 中的默认策略一致。
- [x] 在 `compress.py:117` 的循环/输出之后添加判断：若 `best_size > target_kb` 就打印警告并以非 0 退出码通知用户目标不可达，同时在 `README.md:23` 中写明这一行为，满足 `PLAN.md:7` 中的“明确提示与返回非 0 退出码”标准。
- [x] 基于 `PLAN.md:38` 的最小回归集要求，把常见场景（横幅、透明图、插画/文本、超大图、强制 PNG）和对应命令/预期大小写入 `README.md:41` 之后的示例段落，形成手动回归检查清单供之后快速验证。
- [x] 围绕 `PLAN.md:42` 的批量处理规划，记录今天对“先稳定单图再扩展多文件/目录、明确输出命名/覆盖策略”的需求分层判断（可写进 `PLAN.md:42` 段落尾或 README 末尾），确保后续开发有清晰起点。
